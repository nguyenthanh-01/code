#include <bits/stdc++.h>
using namespace std;

#define n 5
#define sz(a) (int) (a).size()

typedef long long ll;

int main() {
    array<int, n> arr;
    printf("%d \n", sz(arr));
    printf("%d \n", sizeof(arr));
    return 0;
}
