#include <bits/stdc++.h>
using namespace std;

#define ms(a, n) memset(a, n, sizeof(a))
#define sz(a) (int) (a).size()
#define pb push_back
#define pp pop_back

typedef long long ll;
typedef vector<int> vi;

int main() {
    return 0;
}
