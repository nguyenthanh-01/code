#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define fx(n) fixed << setprecision(n)

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    float x = 3.145982;

    cout << fx(2) << x;
    return 0;
}
