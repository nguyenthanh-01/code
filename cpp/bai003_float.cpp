#include <bits/stdc++.h>
using namespace std;

#define fx(a) fixed << setprecision(a)

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    float x = 3.145982;

    cout << fx(2) << x;
    return 0;
}
