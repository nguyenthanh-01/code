#include <bits/stdc++.h>
using namespace std;

#define all(a) (a).begin(), (a).end()
#define rall(a) (a).rbegin(), (a).rend()

typedef long long ll;
typedef struct {int fi, se;} ii;
typedef vector<int> vi;
typedef vector<ii> vii;

inline bool cmp (ii &i, ii &j) {return i.fi < j.se;}

int main() {
    return 0;
}
